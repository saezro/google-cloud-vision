# TensorFlow con soporte CUDA (las libs de NVIDIA vienen como wheels de pip).
# El driver lo pone Cloud Run cuando despliegas con --gpu.
tensorflow[and-cuda]==2.17.0
tensorflow-datasets==4.9.6
importlib_resources   # tfds 4.9.x lo importa pero no lo declara en Python 3.11
scipy                 # tfds lee las etiquetas .mat de Flowers-102 con scipy.io.loadmat

# Pines de protobuf: sin esto, la imagen deja de construirse con el tiempo.
# TF 2.17 exige protobuf<5, pero las versiones recientes de array_record traen
# ficheros _pb2.py generados con protoc>=5.27, que importan
# `google.protobuf.runtime_version` — inexistente en protobuf 4.x. Resultado:
# `tensorflow_datasets has no attribute 'load'` y el job muere al arrancar.
# Reconstruir sin estos pines rompe el entrenamiento (visto el 2026-09-09).
array_record==0.5.1
protobuf==4.25.3
tensorflow-metadata==1.15.0   # 1.16+ genera sus _pb2 con protoc 5 y exige protobuf>=5
